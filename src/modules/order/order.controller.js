import { Cart } from "../../../DB/models/cart.model.js";
import nodemailer from "nodemailer"
import { sendEmail } from "../../utils/sendEmail.js";


export const payItem = async(req, res, next) => {
    // pay

    sendEmail()
}

export const payCart = async(req, res, next) => {
    // pay

    sendEmail()
}